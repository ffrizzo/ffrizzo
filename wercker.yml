box: google/golang

build:
  steps:
    - setup-go-workspace

    - script:
      name: hugo setup
      code: go get github.com/spf13/hugo

    - script:
      name: hugo build
      code: hugo

deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: ffrizzo.github.io
        basedir: public
