box: google/golang

build:
  steps:
    - setup-go-workspace

    - arjen/hugo-build:
      version: 0.14
      theme: material-design
      config: config.toml
      flags: --buildDrafts=true

deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: blog.ffrizzo.com
        basedir: public
