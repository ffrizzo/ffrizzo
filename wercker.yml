box: golang
build:
  steps:
    # golint step!
    - wercker/golint

    - script:
        name: build
        code: hugo

deploy:
  steps:
    - ffrizzo/gh-pages:
        token: $GIT_TOKEN
        domain: ffrizzo.github.io
        basedir: public
