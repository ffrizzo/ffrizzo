box: wercker/default

build:
  steps:
    - arjen/hugo-build:
        version: 0.14
        theme: material-design
        config: config.toml

# deploy:
#   steps:
#     - ffrizzo/gh-pages:
#         token: $GIT_TOKEN
#         domain: ffrizzo.github.io
#         basedir: public
