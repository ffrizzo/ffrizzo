box: golang
build:
  steps:
    - wercker/setup-go-workspace
    - arjen/hugo-build
        theme: material-design
        config: config.toml
        prod_branches: master
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: blog.ffrizzo.com
        basedir: ./public
