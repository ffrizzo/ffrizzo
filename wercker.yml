box: golang
build:
  steps:
    - wercker/setup-go-workspace
    - arjen/hugo-build
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: blog.ffrizzo.com
        basedir: ./public
