box: default
build:
  steps:
    # golint step!
    - wercker/golint

    - arjen/hugo-build:
        name: build
        flags: --buildDrafts=true

deploy:
  steps:
    - ffrizzo/gh-pages:
        token: $GIT_TOKEN
        domain: ffrizzo.github.io
        basedir: public
